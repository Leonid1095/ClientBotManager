# БЫСТРАЯ УСТАНОВКА И ЗАПУСК

## ⚡ Первый запуск (Установка)

### Windows:
```bash
# Просто дважды кликните на файл:
install.bat

# Или в командной строке:
python install.py
```

### Linux/MacOS:
```bash
chmod +x install.sh
./install.sh

# Или напрямую:
python3 install.py
```

## 🚀 Запуск бота после установки

### Windows:
```bash
# Способ 1: Двойной клик
run.bat

# Способ 2: Командная строка
python bot.py
```

### Linux/MacOS:
```bash
./run.sh

# Или:
python3 bot.py
```

## 🎛 Управление ботом (manage.py)

Используйте скрипт управления для простого управления ботом:

```bash
# Интерактивное меню
python manage.py

# Запустить бота
python manage.py run

# Обновить зависимости
python manage.py update

# Просмотр логов
python manage.py logs
```

## ⚙️ Что делает установщик

1. ✓ Проверяет Python (3.8+)
2. ✓ Создаёт виртуальное окружение
3. ✓ Устанавливает зависимости
4. ✓ Конфигурирует бота (токен, user_id и т.д.)
5. ✓ Создаёт скрипты для запуска

## 📋 Требования

- **Python 3.8+**
- **pip** (устанавливается с Python)
- **Интернет** (для загрузки зависимостей)
- **Токен Telegram-бота** (от @BotFather)
- **Ваш Telegram ID** (от @userinfobot)

## 🔧 Конфигурация

После установки отредактируйте `config.py` (если нужно):

```python
TELEGRAM_TOKEN = "ваш_токен"
ADMIN_USER_ID = ваш_user_id
DEVELOPER_EMAIL = "email@example.com"
DEVELOPER_USERNAME = "your_username"
```

## 📍 Структура после установки

```
Бот по заказам/
├── venv/                    # Виртуальное окружение
├── bot.py                   # Основной файл бота
├── config.py                # Конфигурация (создаётся при установке)
├── run.bat / run.sh         # Скрипты для запуска (создаются)
├── install.py               # Умный установщик
├── install.bat / install.sh # Оболочки для установки
├── manage.py                # Менеджер бота
├── requirements.txt         # Зависимости
└── ...остальные файлы...
```

## 🐛 Решение проблем

### Python не найден
- **Windows:** Установите Python с [python.org](https://www.python.org) и выберите "Add Python to PATH"
- **Linux:** `sudo apt-get install python3`
- **MacOS:** `brew install python3`

### Ошибка при установке зависимостей
```bash
# Обновите pip
pip install --upgrade pip

# Повторите установку
python install.py
```

### Бот не запускается
1. Проверьте токен в `config.py`
2. Проверьте интернет-соединение
3. Посмотрите логи: `python manage.py logs`

## 📚 Дополнительно

- [Документация aiogram](https://docs.aiogram.dev/)
- [Telegram Bot API](https://core.telegram.org/bots/api)
- [README.md](README.md) - Полная документация бота
