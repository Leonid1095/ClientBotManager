# 🚀 Развертывание системы бекапов на сервере

## 1️⃣ Обновить код с GitHub

На сервере выполните:

```bash
cd ~/clientbotmanager
git pull origin main
```

## 2️⃣ Перезагрузить бот

```bash
sudo systemctl restart clientbotmanager
```

## 3️⃣ Проверить статус

```bash
sudo systemctl status clientbotmanager
journalctl -u clientbotmanager -f
```

В логах должно быть:
```
🤖 Бот запущен!
Создание начального бекапа...
Бекап создан успешно: /home/plg/clientbotmanager/backups/backup_20260201_143025.zip
Автоматические бекапы включены (каждые 7 дней)
```

## 4️⃣ Настроить интервал бекапов (опционально)

Отредактируйте файл на сервере:

```bash
nano ~/clientbotmanager/config.py
```

Найдите строки:
```python
BACKUP_ENABLED = True              # Включить автоматические бекапы
BACKUP_INTERVAL_DAYS = 7           # Раз в неделю
BACKUP_DIR = "backups"             # Директория для хранения
BACKUP_KEEP_COUNT = 10             # Хранить последние 10 бекапов
```

После изменений перезагрузите бота:
```bash
sudo systemctl restart clientbotmanager
```

## 5️⃣ Использовать админ-команды

В Telegram отправьте администратору:

### Создать бекап немедленно
```
/backup
```

### Показать список бекапов с возможностью восстановления
```
/backup_list
```

### Показать настройки
```
/backup_settings
```

## 6️⃣ Проверить директорию бекапов

```bash
ls -lh ~/clientbotmanager/backups/
```

Вывод должен быть похож на:
```
backup_20260201_143025.zip (45 KB)
backup_20260125_100000.zip (38 KB)
backup_20260118_090000.zip (42 KB)
```

## 🔍 Проверка работы

### Логи автоматического бекапа
```bash
journalctl -u clientbotmanager | grep -i backup
```

### Размер папки бекапов
```bash
du -sh ~/clientbotmanager/backups/
```

### Содержимое последнего бекапа
```bash
unzip -l ~/clientbotmanager/backups/backup_*.zip | tail -1
```

## ⚠️ Важные моменты

1. **Первый бекап** создается автоматически при запуске бота
2. **Рецепт по умолчанию** - раз в 7 дней (настраивается в config.py)
3. **Автоочистка** - старые бекапы удаляются автоматически (ограничение в 10 штук)
4. **Хранение** - бекапы хранятся локально в папке `backups/`

## 📋 Чек-лист

- [ ] Обновили код (`git pull origin main`)
- [ ] Перезагрузили бот (`sudo systemctl restart clientbotmanager`)
- [ ] Проверили логи (в них есть "Автоматические бекапы включены")
- [ ] Создали первый бекап вручную (`/backup`)
- [ ] Проверили список бекапов (`/backup_list`)
- [ ] Настроили интервал (если нужно)

## 📞 Если что-то не работает

### Бекапы не создаются
```bash
# Проверьте права доступа
ls -ld ~/clientbotmanager/backups/

# Проверьте логи ошибок
journalctl -u clientbotmanager -n 50
```

### Нет команды /backup
- Убедитесь, что вы администратор (проверьте ADMIN_USER_ID в config.py)
- Отправьте `/start` в бот
- Перезагрузите бот (`sudo systemctl restart clientbotmanager`)

### Много занимает места
```bash
# Удалите старые бекапы вручную
rm ~/clientbotmanager/backups/backup_2025*.zip

# Или измените BACKUP_KEEP_COUNT в config.py (например, на 5)
```

---

**✅ Готово! Система бекапов полностью интегрирована и работает 24/7**
