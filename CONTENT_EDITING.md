# 📝 Руководство по редактированию контента бота

Здесь объяснено, где и как редактировать информацию на разных экранах бота.

---

## 🏠 Главное меню и О себе

### Файл: `config.py`

```python
# Ваши контакты
TELEGRAM_TOKEN = "ВАШ_ТОКЕН_БОТА"
ADMIN_USER_ID = 123456789  # Ваш user_id
```

### Файл: `bot.py` (примерно строка 220)

Найдите функцию `handle_about()`:

```python
@dp.message_handler(lambda m: m.text == ABOUT_TEXT)
async def handle_about(message: types.Message):
    """Информация о компании"""
    await message.answer(
        "👤 <b>О себе</b>\n"
        "Я — разработчик Telegram-ботов с опытом 3+ года. "
        "Более 50 реализованных проектов для бизнеса и частных лиц.\n\n"
        "Портфолио и отзывы доступны в соответствующих разделах.",
        parse_mode="HTML",
        reply_markup=get_back_keyboard()
    )
```

**Отредактируйте текст в кавычках** - напишите о себе вместо этого примера.

---

## 📞 Связаться с разработчиком

### Файл: `bot.py` (примерно строка 230)

Найдите функцию `handle_contact_dev()`:

```python
@dp.message_handler(lambda m: m.text == CONTACT_TEXT)
async def handle_contact_dev(message: types.Message):
    """Контакты разработчика"""
    await message.answer(
        "📞 <b>Контакты</b>\n"
        "Telegram: @ваш_ник\n"
        "Email: email@example.com",
        parse_mode="HTML",
        reply_markup=get_back_keyboard()
    )
```

**Обновите контакты:**
- `@ваш_ник` - ваш ник в Telegram (найдите в настройках профиля)
- `email@example.com` - ваш email
- Можете добавить номер телефона, Whatsapp и т.д.

---

## 💼 Портфолио - примеры работ

### Файл: `portfolio.py`

Полностью открыт для редактирования! Каждый кейс содержит:

```python
{
    "id": "1",                    # Уникальный ID
    "title": "🍕 Название",      # Название кейса (с emoji)
    "desc": "Краткое описание",   # Краткое описание (1-2 строки)
    "details": "Полное описание"  # Полное описание с деталями
}
```

**Примеры в файле:**
1. 🍕 Бот для доставки еды
2. 📅 Бот для консультаций
3. 💼 Бот для управления проектами
4. 📚 Обучающий бот

**Как отредактировать:**
1. Откройте `portfolio.py`
2. Замените примеры на ваши реальные проекты
3. Добавьте emoji для каждого кейса
4. Напишите детали проекта (задача, решение, результат)

**Пример отредактированного кейса:**

```python
{
    "id": "my_project",
    "title": "🛍️ Интернет-магазин",
    "desc": "Бот для продажи одежды с каталогом и корзиной",
    "details": (
        "<b>Задача:</b> Создать бот для продажи одежды.\n\n"
        "<b>Решение:</b>\n"
        "• Каталог товаров\n"
        "• Корзина и оформление покупки\n"
        "• Интеграция с платежами\n\n"
        "<b>Результат:</b> За 3 месяца 500+ продаж."
    )
}
```

---

## ❓ FAQ - часто задаваемые вопросы

### Файл: `faq.py`

Содержит вопросы и ответы. Структура:

```python
{
    "q": "❓ Вопрос?",
    "a": "Ответ на вопрос"
}
```

**Текущие вопросы в файле:**
- 💰 Сколько стоит разработка?
- ⏱️ Сколько времени занимает?
- ✅ Какие гарантии?
- 🔧 Какие технологии?
- 📱 Будет ли работать на мобильных?
- 🚀 Как долго работает бот?
- 📞 Способы общения?

**Как отредактировать:**
1. Откройте `faq.py`
2. Замените вопросы и ответы на ваши
3. Сохраните файл

**Совет:** Используйте emoji перед вопросом для лучшей видимости.

---

## ⭐ Отзывы клиентов

### Файл: `reviews.py`

Содержит опубликованные отзывы. Структура:

```python
{
    "id": "rev_001",           # Уникальный ID
    "author": "Имя",           # Имя автора
    "rating": 5,               # Рейтинг 1-5
    "text": "Текст отзыва",    # Текст отзыва
    "date": "2026-02-01"       # Дата
}
```

**Как добавить отзыв:**
1. Откройте `reviews.py`
2. Добавьте новый элемент в список `REVIEWS`:

```python
{
    "id": "rev_006",
    "author": "Дмитрий",
    "rating": 5,
    "text": "Отличная работа! Боту уже 6 месяцев.",
    "date": "2026-02-15"
}
```

### Система модерации отзывов

Клиенты оставляют отзывы через кнопку в боте, но они **не публикуются сразу**.

**Процесс:**
1. Клиент нажимает "✍️ Оставить отзыв"
2. Пишет отзыв
3. Отзыв попадает в **очередь модерации** (защита от спама)
4. Администратор видит уведомление в боте
5. Администратор может **одобрить** (✅) или **отклонить** (❌)

**Админ-команда:** `/reviews_pending`
- Показывает все отзывы на модерацию
- Кнопки для одобрения/отклонения

**Примечание:** Отзывы хранятся в памяти бота, при перезагрузке очередь очищается (сохраняются только в бекапах).

---

## 🎨 Форматирование текста

В боте поддерживается HTML-форматирование:

```html
<b>Жирный текст</b>
<i>Курсив</i>
<u>Подчеркнутый</u>
<code>Моноширинный текст</code>

Перевод строки: \n
```

**Пример:**

```python
"<b>Заголовок</b>\n"
"Текст с <i>курсивом</i>\n"
"<code>код_или_ссылка</code>"
```

---

## 📋 Чек-лист для редактирования

- [ ] Обновили `config.py` - ADMIN_USER_ID и контакты
- [ ] Отредактировали "О себе" в `bot.py`
- [ ] Добавили свои контакты (Telegram, Email)
- [ ] Заполнили `portfolio.py` - ваши реальные проекты
- [ ] Обновили `faq.py` - ответы на вопросы
- [ ] Добавили реальные отзывы в `reviews.py`
- [ ] Протестировали все разделы в боте

---

## 🚀 После редактирования

1. **Локально:**
   ```bash
   python bot.py
   ```
   - Протестируйте все разделы

2. **На сервере:**
   ```bash
   cd ~/clientbotmanager
   git add .
   git commit -m "Обновлена информация в боте"
   git push origin main
   sudo systemctl restart clientbotmanager
   ```

3. **Проверьте в Telegram:**
   - Отправьте `/start`
   - Проверьте все меню и кнопки
   - Убедитесь, что ваша информация везде правильная

---

## 💡 Советы

1. **Emoji:** Используйте emoji для красивого оформления
2. **Краткость:** Описания должны быть короткими и ёмкими
3. **Честность:** Отзывы должны быть реальными
4. **Актуальность:** Регулярно обновляйте портфолио и цены
5. **Резервная копия:** Перед редактированием сделайте `git commit`

---

**Все готово! Начните редактировать и кастомизировать бота под себя!** ✨
